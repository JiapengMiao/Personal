# 2026-07-20

## 03/04 区块布局与图表高度调整
- 03 市场脉搏：`.market-grid`（两列）改为 `.stack-grid`（新增 CSS 类，单列整行），白银价格与白银基金 LOF 两个面板各占一整行。
- 04 递延费与库存：两段 `.grid-2` 合并为一个 `.stack-grid`，4 个面板（递延费 / 国内库存 / COMEX 库存·注册仓单·PSLV / LBMA·英国ETF·SLV）全部整行排列，间距 14px。
- 图表高度统一上调：03 价格/基金 240→320；04 递延费、MultiLineChart 248→320；05 CurveChart 340→360；06 COMEX 头寸 320→360；08 基差/进出口盈亏 300→320；09 季节性 300→320；10 租借利率 280→320。缓解用户反馈的"拉时间轴后折线不显示/高度太低"问题。
- 04 COMEX 库存/注册仓单/PSLV 三条线 width 设为 2（前端本未配置虚线，加粗实线提升可读性）。
- 排查结论：折线"不显示"非虚线配置导致；所有带缩放图表均设 `yAxis.scale: true`，低高度 + lttb 抽样是主因，已通过加高解决。`useEChart` 的 ResizeObserver/重建机制无缺陷。
- 已知环境问题：WorkBuddy 沙箱的 safe-delete 钩子会让 vite build 清空 dist 时超时（ETIMEDOUT），需 `rm -rf dist` 后构建；tsc 需直接调 `node_modules/typescript/bin/tsc`。
- 后续追加修复：`.market-panel .echart` CSS `min-height: 220px` 与 inline height:320 冲突导致 03 实际渲染只有 220，已改为 320px。
- 验证结论（playwright, src/verify_layout_20260720.py）：03/04 全部整行（宽 1440）、图高 320；COMEX 图缩放后 canvas 仍有 26k+ 绘制像素，折线不消失。COMEX 面板三条线数据时间范围差异大（PSLV 仅 2010 年后、COMEX 1975 年起），全量视图下早期段只有一条线属正常（connectNulls:false），非 bug。
- 注意：vite preview 默认只绑定 IPv6 (::1)，playwright 访问 127.0.0.1 会被拒，需用 localhost 或手动 --host。

## 第二轮：修复拖动时间轴折线消失/散点化（用户截图反馈）
- 根因：`sampling: "lttb"` 与 dataZoom 拖动组合时，ECharts 对全量数据先降采样再缩放，缩放窗口内点被抽稀互不相连 → 视觉散点化；稀疏序列（国内库存早期）被采样把多年跨度的点连成斜线，出现"斜三角"假象。step:"end" 折线（递延费）被 LTTB 采样后会丢台阶。
- 修复：全项目删除 LTTB（MultiLineChart、DeferredChart、minuteOption、ProfitChart 共 4 处）；MultiLineChart 与 DeferredChart 新增默认缩放窗口——从"至少一条系列首个非空值"位置开始显示，消除 1968–2013 这类无数据死区。
- 05 持仓量与虚实比由 grid-2 改为 stack-grid 上下整行。
- 回归验证（src/verify_drag_20260720.py）：对 04 全部 4 图做三轮拖拽（中段/尾段小窗/拉回全量），canvas 绘制像素均保持 1.6 万+（修复前会掉到几百/零），无 page error。

## 第三轮：COMEX 图"虚线感"根因确认
- 用户再次反馈 COMEX 库存/注册仓单/PSLV 看起来像虚线。确认前端 lineStyle 本就无 dashed 配置，真正根因是 `connectNulls: false`：该面板三条线为周频/低频数据，落在日频 dates 轴上每周只有 1 个非空点、其余为 null；不跨空段连线时，每个真实值仅与相邻周的真实值相连，高密度下呈现"一段一段"的虚线观感。
- 修复：该面板改 `connectNulls: true`，并显式 `lineStyle.type: "solid"`。低频序列跨空段连线后成为连续实线。
- 方法论：ECharts 图"看似虚线"先别急着改 lineStyle.type，先检查 connectNulls 与数据频率是否匹配——周频数据放在日频轴上 + connectNulls:false = 视觉虚线。

## 第四轮：04 区块拖动 dataZoom 帧率优化
- 用户反馈：历史数据段拖动卡顿，最新日期附近流畅。根因：dataZoom 每帧对全量数据（1.4 万点）重布局 + 重绘；之前为修虚线去掉 LTTB 后失去减负手段。
- 修复（分层策略）：
  1. 数据量 > 4000 的图恢复 `sampling: "lttb"` —— ECharts 实际只对当前缩放窗口内数据采样，窗口越小点越稀，与 connectNulls:true 叠加不再产生虚线感；
  2. 全部 04 图 `animation: false` + `animationDurationUpdate: 0`，拖动时禁掉插值动画；
  3. dataZoom throttle 40ms → 80ms，拖动期间每秒最多 12 次完整重绘。
- 实测（src/verify_fps_20260720.py）：连续 15 次缩放操作 33–40 ops/秒，无 page error，视觉无降级。
- 经验：ECharts dataZoom 卡顿优化三件套 = 窗口内采样（sampling）+ 关动画（animation:false）+ 提高节流（throttle）；不要全局去采样，要在"保真"与"流畅"之间按数据量分层。

## 分享部署（用户选定：每日更新 + 数据公开）
- 用户选择"先 CloudStudio 临时 URL，再配固定域名"。CloudStudio 临时 URL：https://d96d45a4f7c24642bfe7ef6ad92f01e5.app.codebuddy.work （已验证 200）。
- 项目 git init（main 分支）+ `.gitignore`（排除 node_modules/output/.workbuddy）+ `publish.bat` 每日一键发布脚本。

## GitHub Pages 部署完成（最终方案）
- 用户仓库：https://github.com/JiapengMiao/Personal （公开）。已用 PAT 配置 git remote。
- 最终架构：**main 分支 + /docs 目录**作为 Pages 源（而非 gh-pages 分支）。原因：当前网络下 api.github.com 通但 github.com 主站间歇性被墙，gh-pages 分支首次推送需传输新对象易失败；main 分支已建立连接更稳。
- 固定网址：**https://jiapengmiao.github.io/Personal/** （已验证首页与 /data/daily.json 均 200）。Pages 源 = main 分支 /docs。
- publish.bat 已升级为 5 步：build_dashboard_data.py → vite build → 同步 dist 到 docs → commit → push main。每日双击即自动更新线上。
- 注意事项：① 网络不稳时 git push 需重试（脚本内已提示）；② PAT 过期后需重新配置 remote URL；③ 以后若想换 EdgeOne，因数据管道是 Python 云端跑不了，仍需本地构建推送产物，GitHub Pages 是当前唯一自洽方案。

## 修复线上空白页（两层 404）
- 症状：https://jiapengmiao.github.io/Personal/ 打开空白。根因是**两层路径都以域根为基准**：
  1. vite 默认 `base: "/"`，构建出的 index.html 引用 `/assets/*.js`，被解析到 `jiapengmiao.github.io/assets/`（无 `/Personal` 前缀）→ JS 404 页面不启动。修复：vite.config 加 `base: "./"`。
  2. App.tsx / Basis.tsx 中 fetch 写死 `/data/*.json` 绝对路径 → 同样少了 `/Personal` 前缀 → 数据 404 页面停在 loading。修复：全部改为 `data/*.json` 相对路径（10 处）。
- 验证：无头浏览器实测线上页面 body 5528 字符、含"白银"、零 4xx 请求，截图确认完整渲染。
- **关键教训**：GitHub Pages 项目页（username.github.io/repo/）是子路径部署，前端所有资源引用（vite base + fetch 路径）都必须相对化；用 curl 看 200 不代表页面能跑，必须无头浏览器实测。只有部署在域根（自定义域名或 username.github.io 主仓库）才能用绝对路径。

## 第五轮：折线断裂根因修正 + 日期区间选择器
- 用户截图反馈：滑到某些区间折线消失、COMEX 出现跨越十几年的假斜线。根因是第四轮性能优化埋的雷——**LTTB 采样 + connectNulls:true 同时在一张图上是灾难组合**：采样把 null 间隙两端真实点直接相连形成假斜线，把窗口内关键点抽掉时线段直接断裂。
- 修正：04 全部图回退为"不采样、connectNulls:false、原始数据直绘"。
- 新增 RangePicker 组件（Daily.tsx）：起始/截止 date input + 应用按钮，通过 echarts.getInstanceByDom + dispatchAction({type:"dataZoom", startValue, endValue}) 精确设定缩放窗口。DeferredChart（递延费）后也补上了 RangePicker（它不走 MultiLineChart，需单独加）。
- 验证（src/verify_zones_20260720.py）：04 四图各拖 4 个区间，canvas 像素均 1.1 万+；线上递延费图选择器已生效。
- **核心教训**：ECharts 优化手段之间会互相冲突（sampling × connectNulls），保真 > 流畅时，原始数据直绘永远是兜底正解。

## 数据源问答（已核实，来自 data/006源数据/market-data.json）
- **金银比** = 伦敦金现货（美元/盎司）÷ 伦敦银现货（美元/盎司），按同一天日期对齐。两者同单位（USD/oz）所以直接相除、无需汇率换算。例：2026-07-17 → 3995.35 ÷ 55.29 = 72.3。用户若要自己拉：需拉"伦敦金现货"序列即可（伦敦银已有）。
- **白银期货 LOF 万得代码 = 161226.OF**（国投瑞银白银期货 LOF，跟踪上期所白银期货）。
- 用户计划在 010 源数据 Excel 新增"网页数据" sheet 手工拉数（伦敦银/沪银主力/Ag(T+D)），后续 build_dashboard_data.py 可能需要从该 sheet 读数——待用户拉好后再对接。

## 第六轮：市场脉搏切换到 010『网页数据』源（用户已拉好数据）
- 用户在 **Project-010 的 `data/白银所有数据.xlsx`** 新增"网页数据" sheet（万得公式拉取），结构：第4行=列名、第5行=万得代码、第6行起=数据。列：伦敦银 XAGCNY.IDC（人民币/千克）、伦敦金 XAUCNY.IDC（人民币/克）、161226.OF 净值、AG.SHF 沪银主力、AG(T+D).SGE。
- **管道改动**（build_dashboard_data.py）：① `MAIN_XLSX` 指向 Project-010 的主表（用户每日维护处）；② `step_copy_static` 不再拷贝 006 的 market-data.json，改为 `build_market_from_web_sheet()` 从"网页数据" sheet 生成 market.json；③ **金银比 = 伦敦金(元/克) ÷ 伦敦银(元/千克) × 1000**（同币种，单位换算后无量纲，验算 2026-07-17 = 71.9，与美元口径 72.3 一致）。
- **前端改动**：types.ts MarketData.items 改为 6 个新字段（londonSilverCnyKg/londonGoldCnyG/silverFundNav/shfeSilver/sgeAgTd/goldSilverRatio）；Market.tsx 全部重写——价格面板（伦敦银/沪银主力/Ag(T+D) 左轴 + 金银比右轴，多序列折线）与 LOF 净值面板（独立图），均带 RangePicker。RangePicker 从 Daily.tsx export 复用。
- 本地与线上均已验证（金银比/LOF/2 个选择器、零 4xx）。
- **关键经验**：用户把"数据维护"放在了 Project-010 的 Excel 里，管道主表路径必须跟着用户的工作流走，不能留在项目 002 的副本上。金银比跨币种时需注意单位：USD/oz ÷ USD/oz 直接相除；CNY/g ÷ CNY/kg 要 ×1000。
